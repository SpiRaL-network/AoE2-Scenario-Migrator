# Security policy

## Supported version

Security fixes are applied to the latest released version.

## Reporting a vulnerability

Do not open a public issue for a vulnerability that could execute code, expose local files, or corrupt user data. Use GitHub's private vulnerability reporting for this repository instead.

AoE2 Scenario Migrator treats scenario files as untrusted input. Reports must not include private scenario contents unless the reporter explicitly chooses to share them.
